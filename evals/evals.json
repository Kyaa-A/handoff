{
  "skill_name": "handoff",
  "evals": [
    {
      "id": 1,
      "prompt": "I'm stopping for the day. Use the attached session context to save progress so another agent can continue tomorrow. Write the handoff and return the normal concise confirmation.",
      "expected_output": "A lean save note that selects the unfinished BOM work as the next action, captures clean Git state and the streaming rationale, excludes unrelated UI work, and reports only verification that actually ran.",
      "files": [
        "evals/fixtures/clean-save-context.md"
      ],
      "expectations": [
        "The note has a clearly designated next action to add the UTF-8 BOM option at `src/audit/export.ts:64` and rerun `pnpm test src/audit/export.test.ts`.",
        "The working state explicitly says branch `feat/audit-export` is clean and points to commit `9b7c1d2`.",
        "The decision records both streaming instead of buffering and the reason that exports can exceed 500 MB.",
        "Confirmed verification quotes the exact command `pnpm test src/audit/export.test.ts` and result `12 passed, 12 total`.",
        "The unrelated admin empty-state copy is not promoted into the goal or next action, and no full suite, typecheck, lint, or other unrun check is described as passing."
      ]
    },
    {
      "id": 2,
      "prompt": "Run the attached setup script, change into the emitted `REPO_PATH`, and use the session context to save my progress. Afterward, report the handoff plus `git status --short --untracked-files=all` and the current SHA-256 of the emitted `SCRATCH_PATH` so preservation can be checked.",
      "expected_output": "A dirty-worktree handoff that selects the negative-discount regression case as next, distinguishes session edits from the user's pre-existing scratchpad, and supplies observable evidence that the scratchpad remained untouched.",
      "files": [
        "evals/fixtures/setup-dirty-worktree.sh",
        "evals/fixtures/dirty-worktree-context.md"
      ],
      "expectations": [
        "The note has a clearly designated next action to add the negative-discount regression case and rerun `pnpm test src/invoices/calculate-total.test.ts`.",
        "The working state names branch `fix/invoice-rounding` and accurately reports both modified invoice files and untracked `notes/customer-cases.txt`.",
        "The note identifies `notes/customer-cases.txt` as pre-existing/user-owned work to preserve; the reported after-operation SHA-256 equals `SCRATCH_SHA256_BEFORE`, and `git status --short` still lists the file as untracked.",
        "The decision states that tax is rounded after summing because invoice INV-1842 differs by one cent under per-line rounding.",
        "Confirmed verification includes the exact focused command and `6 passed, 6 total`; the PDF-logo distractor is not promoted into current work, and the full `pnpm test` suite is not described as passing."
      ]
    },
    {
      "id": 3,
      "prompt": "We're pausing this feature. Read the attached session context and leave a handoff that a fresh agent can act on without the conversation history.",
      "expected_output": "A save note that independently separates committed, incomplete, and uncertain work; records dirty state and database-concurrency rationale; ignores deferred logging cleanup; and chooses the conflict lookup plus real-Postgres test as next.",
      "files": [
        "evals/fixtures/partial-feature-context.md"
      ],
      "expectations": [
        "The note has a clearly designated next action to implement the unique-conflict lookup in `src/dispatch/idempotency.repository.ts` and then run the real-Postgres idempotency integration test.",
        "The note identifies commit `71c4e2a` as completed but describes the repository conflict path and its current `duplicate request` throw as incomplete.",
        "The working state names branch `feat/idempotent-dispatch`, modified `src/dispatch/dispatch.service.ts`, and untracked `src/dispatch/idempotency.repository.ts`.",
        "The Postgres uniqueness choice includes its rationale that multiple API replicas process requests concurrently; deferred log-copy cleanup is not promoted into current scope.",
        "Only `pnpm typecheck` with `Done in 4.2s` is confirmed; migration behavior is explicitly unverified, and no unit, integration, or end-to-end test is described as passing."
      ]
    },
    {
      "id": 4,
      "prompt": "Run the attached setup script and change into its emitted `REPO_PATH`. Then resume from the attached handoff. Inspect only what is needed to validate its pointers, show the commands and results used, state the short resume plan, and take no implementation action yet.",
      "expected_output": "A targeted resume based on actual repository inspection that detects the branch mismatch, unverifiable commit, moved file, and already-applied signal change without repeating the obsolete edit or claiming a current test pass.",
      "files": [
        "evals/fixtures/setup-stale-repo.sh",
        "evals/fixtures/stale-handoff.md"
      ],
      "expectations": [
        "Command evidence from the emitted repository shows the checked-out branch is `fix/export-timeout-v2`, conflicting with the handoff's `fix/export-timeout`.",
        "Command evidence shows `git rev-parse --verify abc1234^{commit}` fails and `src/export/run-export.ts` does not exist.",
        "Targeted file inspection finds `src/exports/export-runner.ts` and shows it already passes `signal` to `fetchBatch`, so the response does not propose the obsolete edit.",
        "The resume plan designates running `pnpm test src/export/run-export.test.ts` as the next verification rather than performing broad repository rediscovery.",
        "The handoff's historical 8/8 result remains distinct from current evidence; because the test is not run by the setup or prompt, the response does not claim it currently passes."
      ]
    },
    {
      "id": 5,
      "prompt": "Run the attached setup script, change into its emitted `DESTINATION_PATH`, and resume from the attached cross-machine context. Show the Git commands and observations used to recover and verify the portable work before making any edits. Stop once you have stated the next implementation/test action.",
      "expected_output": "An offline cross-machine resume that observes the destination state, fetches the feature branch from the local bare remote, verifies the emitted feature commit before any edit, avoids source-only artifacts, and treats prior test output as historical.",
      "files": [
        "evals/fixtures/setup-cross-machine-repos.sh",
        "evals/fixtures/cross-machine-context.md"
      ],
      "expectations": [
        "Before fetching, command evidence shows the destination is on clean `main` and the emitted `FEATURE_SHA` is not available in the destination object database.",
        "The recorded operation order shows `git fetch origin feat/document-cursors` occurs before successful `git cat-file -e <FEATURE_SHA>^{commit}` or equivalent commit verification, and both occur before any checkout or edit.",
        "After fetch, command evidence ties the fetched commit (`FETCH_HEAD` or an explicitly created remote-tracking ref) to the emitted `FEATURE_SHA`; the response does not merely repeat the context's claim that it was pushed.",
        "The response does not access or reconstruct the source checkout's `tmp/page-debug.json`, and preserves the rationale that `(created_at, id)` gives deterministic ordering when timestamps are not unique.",
        "The source-machine `9 passed, 9 total` result is labeled historical; `pnpm test src/documents/cursor.test.ts` is designated for a local rerun, and no full-suite or destination test pass is claimed."
      ]
    }
  ]
}
